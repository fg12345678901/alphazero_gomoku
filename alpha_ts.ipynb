import torch
from network.ascend_om_net_ais import AscendOMNetAIS

net = AscendOMNetAIS("models/alpha_zero.om", device_id=0)
x = torch.zeros(1, 7, 15, 15, dtype=torch.float32)  # 一步空棋盘
policy, value = net(x)
print(policy.shape, value.shape)  # 预期: torch.Size([1, 225]) torch.Size([1])

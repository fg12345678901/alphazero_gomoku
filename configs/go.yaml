rules:
  board_size: 19
  komi: 7.5
  history_steps: 8

search:
  mcts_sims: 300
  cpuct: 1.5
  dirichlet_alpha: 0.03
  dirichlet_eps: 0.25
  selfplay_temperature: 1.0
  n_temp_moves: 30
  eval_games: 50

model:
  channels: 256
  num_res: 15

train:
  buffer_size: 800000
  batch_size: 512
  train_updates: 8000
  learning_rate: 0.0008
  weight_decay: 0.0001
  selfplay_games: 500

runtime:
  model_dir: models
  data_dir: data
  log_dir: logs
  tb_dir: tb

logging:
  level: INFO
  name: alphazero
